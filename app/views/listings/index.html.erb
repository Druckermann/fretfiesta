<div class="d-flex justify-content-center">
  <h1>Rent a guitar!</h1>
</div>

<div class="container">
  <div class="row">
    <% @listings.each do |listing| %>
      <div class="col-md-4">
        <div class="card-list my-2 p-2">
          <%= link_to listing.name, listing_path(listing) %>
        </div>
      </div>
    <% end %>
  </div>
</div>
